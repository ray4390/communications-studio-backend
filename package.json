{
  "name": "communications-studio-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --import ./src/instrument.js src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/db.js && node --check src/policy.js && node --check src/providers.js && node --check src/routing.js && node --check src/publish.js && node --check src/instrument.js"
  },
  "dependencies": {
    "@sentry/node": "^10.69.0",
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
